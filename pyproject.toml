[project]
name = "chainlit"
version = "0.1.0"
description = ""
authors = [
    {name = "Sarun-Yawichai",email = "sarun.yawichai@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "some-package (>=0.1,<0.2)"
]

[tool.poetry]

[tool.poetry.group.dev.dependencies]
pytest = "^8.4.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
